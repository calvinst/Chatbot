## Como rodar

### Backend
1. Entre na pasta `backend/`
2. Copie o arquivo de exemplo: `cp .env.example .env`
3. Coloque sua chave do Gemini no `.env`
4. `npm install`
5. `node server.js`

### Frontend
1. Entre na pasta `frontend/`
2. `npm install`
3. `npm run dev`
